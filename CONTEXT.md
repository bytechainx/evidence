# evidence 上下文

本文件定义 `evidence` 与其使用方共享的核心词汇。它只记录领域含义与能力边界，
不记录具体实现、API 签名、存储或部署决定。

## 记录与身份

**证据记录**：领域无关的规范化决策证据，由 event / code_version / input_snapshot /
raw_batch_ids / result_digest / outcome 六个字段确定；它不承载决策 DTO 本身。
_Avoid_: 日志行、审计事件（日志没有行格式与摘要约束，审计事件含义过宽）

**规范化行**：由 schema 标识与固定字段次序构成的单行 payload；它同时充当幂等键，
因此格式变更属于 wire 破坏性变更。
_Avoid_: 序列化格式（不能表达「同时是幂等键」这一双重身份）

**输入快照指纹**：由输入域负责计算、本 crate 只负责固化的输入标识；本 crate 既不产生
也不校验其内容。
_Avoid_: 数据版本（不能表达「由谁计算」这一责任划分）

**原始批次标识**：参与计算的原始批次引用，顺序具有确定性语义；本 crate 只保存引用，
不保存批次内容。
_Avoid_: 文件路径、数据源（批次是不可变输入单位，路径不是身份）

**结果摘要**：决策结果或 payload 的 64 位小写十六进制 SHA-256 文本。
_Avoid_: 结果内容（本 crate 不保存结果本体，只保存其摘要）

## 追加与回执

**追加 seam**：本 crate 唯一的写入面，领域与应用通过它注入实现；领域 DTO 的所有权留在领域层。
_Avoid_: Repository（暗示领域模型持久化，而这里是领域无关的追加面）

**追加回执**：一次已确认追加的产物，含单调序号、完整记录与可选 receipt 绑定。
_Avoid_: 主键、row id（回执绑定的是追加事实，不是可更新的行身份）

**单调追加序号**：从 1 起严格递增的本地序号；重开存储时逐行校验并从最大序号继续。
_Avoid_: 时间戳序号（时钟不可靠，不能表达严格递增）

**幂等重放**：同一规范化记录已存在时复用既有回执，而不是新增一行；未实现该能力的适配器
默认拒绝调用，避免调用方把普通追加误认为可安全重试。
_Avoid_: 去重（去重不表达「能力必须显式声明、否则拒绝」）

**receipt 绑定**：由组合根在追加时注入的 40 位 commit + environment + artifact 摘要，
与记录一并固化并被校验。
_Avoid_: 元数据（绑定是被校验的固定字段集，不是自由标签）

## 持久性与一致性

**持久性能力**：由实现显式声明的持久化强度（`Volatile` / `LocalDurable` / `RemoteDurable`），
调用方据此决定是否允许在生产路径使用。
_Avoid_: 可靠性等级（不能表达「由实现声明、由调用方准入」）

**结果未知**：远程追加无法确认是否提交时的显式结局；调用方必须保留回执并在远程恢复后
幂等重试或查询。
_Avoid_: 失败（失败表示确定未写入，结果未知是明确的不确定）

**锁残留 fail-closed**：异常退出后不猜测锁是否过期，宁愿拒绝服务也不允许两个进程写同一文件。
_Avoid_: 超时自动释放（会把「不确定」当成「安全」）

**只读查询面**：不触发写入的检索面，与追加 seam 解耦；它按序号或规范化记录检索回执。
_Avoid_: 索引（本 crate 不提供索引、查询授权或远程检索）

## 溯源与批准

**溯源绑定**：记录 provider 请求标识 → raw SHA → lineage → PIT 的结构性绑定；本 crate
只校验字段存在与格式合法，不做 provider 侧内容级对账。
_Avoid_: 数据血缘（血缘常指全图，这里只把单次请求绑到单个时间点）

**决策合成**：一个决策标识与其全部输入溯源绑定的组合视图；它表达「该决策由哪些溯源构成」。
_Avoid_: 决策记录（决策 DTO 的所有权留在领域层）

**受保护签名**：字段私有、只能由 `sign_canonical` 构造的 Owner / Reviewer canonical 签名，
不可从外部绕过不变量拼装。
_Avoid_: 数字证书（不是 PKI；签名由注入密钥的摘要方案产生）

**审批回读**：校验 Owner 先签、Reviewer 再批的状态迁移；角色互换或缺席即拒绝。
_Avoid_: 权限校验（审批是签名角色语义，不是 ACL）
